[user]
    email = timothykeith@gmail.com
    name = Timothy Keith
#    signingkey = 4135C5931D89368E7F32C8EDF4F4A135C022EE12
    signingkey = 9B9B704B1DEFF6C64F4672FC57801331E79295BD

[color]
    diff = auto
    status = auto
    branch = auto
    interactive = auto
	ui = true

[merge]
    tool = vimdiff
[mergetool]
    prompt = true
[mergetool "vimdiff"]
    cmd = nvim -d $LOCAL $REMOTE $MERGED -c '$wincmd w' -c 'wincmd J'
[difftool]
    prompt = false
[diff]
    tool = vimdiff

[core]
	excludesfile = ~/.gitignore

[receive]
    denyCurrentBranch = updateInstead
    
[alias]
    history = log --graph --pretty=format:'%Cred%h%Creset -%C(yellow)%d%Creset %s %Cgreen(%cr) %C(bold blue)<%an>%Creset' --abbrev-commit --date=relative --branches --date-order
	pushall = !git remote | xargs -L1 git push --all
[gpg]
	program = /usr/bin/gpg2
[credential]
	helper = cache
